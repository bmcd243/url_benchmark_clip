# @package agent
_target_: agent.semantic_apt.SemanticAPTAgent
name: semantic_apt
reward_free: ${reward_free}
obs_type: ??? 
obs_shape: ??? 
action_shape: ??? 
device: ${device}
lr: 1e-4
critic_target_tau: 0.01
update_every_steps: 2
use_tb: ${use_tb}
use_wandb: ${use_wandb}
num_expl_steps: ??? 
hidden_dim: 1024
feature_dim: 50
stddev_schedule: 0.2
stddev_clip: 0.3
nstep: 3
batch_size: 256
init_critic: true
update_encoder: ${update_encoder}

# --- APT KNN Parameters ---
knn_rms: true
knn_k: 12
knn_avg: true
knn_clip: 0.0001

# --- CLIP Parameters ---
clip_model_name: "ViT-B-32" 
clip_pretrained: "laion2b_s34b_b79k"

# python pretrain.py agent=semantic_apt obs_type=pixels domain=walker