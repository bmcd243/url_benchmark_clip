# agent/lsd.yaml
# @package agent
_target_: agent.lsd.LSDAgent
name: lsd
reward_free: ${reward_free}
obs_type: ???
obs_shape: ???
action_shape: ???
device: ${device}
lr: 1e-4
critic_target_tau: 0.01
update_every_steps: 2
use_tb: ${use_tb}
use_wandb: ${use_wandb}
num_expl_steps: ???
hidden_dim: 1024
feature_dim: 50
stddev_schedule: 0.2
stddev_clip: 0.3
nstep: 3
batch_size: 1024
init_critic: true
update_encoder: ${update_encoder}

# LSD-specific
skill_dim: 8      # continuous, on unit sphere - start smaller than DIAYN's 16
lsd_scale: 1.0